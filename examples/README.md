# Examples

Small helper scripts/snippets for developing Meeting Buddy.

## `ws_tail.py`
A minimal WebSocket client that connects to the backend and prints snapshot/update events.

```bash
python -m pip install websockets
python examples/ws_tail.py
```
