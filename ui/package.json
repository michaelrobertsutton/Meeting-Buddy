{
  "name": "meeting-buddy-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "build-frontend": "mkdir -p dist/src && cp index.html dist/ && cp src/* dist/src/"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
